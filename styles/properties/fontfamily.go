package properties

// FontFamily selects the text typeface (CSS font-family). The family
// must exist in the registered font collection; the built-in Go fonts
// register the "Go" family.
type FontFamily string
